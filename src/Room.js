class Room {
  constructor(roomNumber, roomType, hasBidet, bedSize, numBeds) {
    this.number = roomNumber;
    this.roomType = roomType;
    this.hasBidet = hasBidet;
    this.bedSize = bedSize;
    this.numBeds = numBeds;
  }
}

export default Room;
